var expect = require('chai').expect,
    RTMClient = require('../lib/rtm');

describe('RTMClient', function () {
    describe('getAPISig', function () {
        it('should return a valid MD5 hash');
        it('should ignore key order in options');
    });
});